# These are automatically applied during `npm run build`
# No need to set them manually in scripts

# Suppress source map warnings from node_modules like @zxing
GENERATE_SOURCEMAP=false

# Prevent ESLint warnings from failing the build
CI=false
